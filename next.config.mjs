/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'firebasestorage.googleapis.com',
        'bookstore-331fc.appspot.com'
      ],
       },
};

export default nextConfig;
